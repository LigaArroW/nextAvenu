import { Person } from "@/lib/auth/authAction";


interface IModelSettings {
    person: Person
}

const ModelSettings: React.FC<IModelSettings> = ({ person }) => {
    return (
        <div>

        </div>
    );
};

export default ModelSettings;